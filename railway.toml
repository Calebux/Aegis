[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build --workspace=apps/dashboard"

[deploy]
startCommand = "npm run start --workspace=apps/dashboard"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
